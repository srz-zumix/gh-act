{
    "description": "${HEAD_REPO_DESCRIPTION}",
    "master_branch": "${HEAD_REPO_DEFAULT_BRANCH_REF}",
    "pusher_type": "user",
    "ref": "$(git symbolic-ref --short HEAD 2>/dev/null || git name-rev --name-only HEAD)",
    "ref_type": "branch",
    "repository": ${HEAD_REPO_JSON},
    "sender": ${HEAD_USER_JSON}
}
