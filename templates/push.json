{
    "after": "${HEAD}",
    "base_ref": null,
    "before": "${BEFORE}",
    "commits": [
        ${COMMITS}
    ],
    "compare": "https://${GITHUB_HOSTNAME}/${OWNER}/${REPO}/compare/${BEFORE:0:12}...${HEAD:0:12}",
    "created": false,
    "deleted": false,
    "forced": false,
    "head_commit": ${COMMITS},
    "pusher": {
        "email": "$(git config --get user.email)",
        "name": "$(git config --get user.name)"
    },
    "ref": "${HEAD_REF}",
    "repository": ${HEAD_REPO_JSON},
    "sender": ${HEAD_USER_JSON}
}
