{
    "action": "synchronize",
    "after": "${HEAD}",
    "before": "${BEFORE}",
    "number": ${PR_NUMBER},
    "pull_request": ${PULL_REQUEST_JSON},
    "repository": ${BASE_REPO_JSON},
    "sender": ${HEAD_USER_JSON}
}
